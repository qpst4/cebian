package com.slideindex.app.imageeditor

/** Stored in [com.slideindex.app.settings.AppSettings.defaultImageViewerPackage] when using the built-in editor. */
object ImageEditorOpenTargets {
    const val BUILTIN_PACKAGE = "com.slideindex.app.imageeditor.builtin"
}
